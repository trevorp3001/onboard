<%= simple_form_for @room do |form| %>

	<%= form.input :title %>
	<%= form.input :address %>
	<%= form.input :description %>
	<%= form.input :image %>
	<%= form.input :bedrooms %>
	<%= form.input :max_guests %>
	<%= form.input :price %>

	<%= form.button :submit, "Book" %>

<% end %>